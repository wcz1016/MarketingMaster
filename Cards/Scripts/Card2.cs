using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Card2 : CardAddress
{
    // Start is called before the first frame update
    private void Awake()
    {
        cardRefer = 25;
        level = 1;
        cardCustlv = 25;
        people = 5;
        cardCost = 5000;
    }
}
